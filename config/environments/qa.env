# Purpose: QA environment values used by the default test run.
# Why used: QA is usually the main automation target before release validation.
BASE_URL=https://www.saucedemo.com
SAUCE_USERNAME=standard_user
SAUCE_PASSWORD=secret_sauce
