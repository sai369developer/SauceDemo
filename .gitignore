# Purpose: Prevent generated dependencies, reports, and local artifacts from being committed.
# Why used: Source control should contain framework code, not machine-specific or generated files.
node_modules/
allure-results/
reports/
!reports/.gitkeep
assets/screenshots/*
!assets/screenshots/.gitkeep
.env
